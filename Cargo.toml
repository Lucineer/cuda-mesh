[package]
name = "cuda-mesh"
version = "0.1.0"
edition = "2021"
description = "Service mesh — service discovery, load balancing, health checking, and traffic routing for agent fleets"

[dependencies]
serde = { version = "1", features = ["derive"] }
